#include "main.h"

/**
 * convert_size_number - Converts 'num' based on 'size'.
 * @numz: The number to be converted.
 * @sizez: The size specifier.
 * Return: num The converted number.
 */

long int convert_size_number(long int numz, int sizez)

{
	UNUSED(sizez);
	/*
	 *  ng 'num' based on 'size' goes here
	 */
	return (numz);
}

/**
 * convert_size_unsgnd - Converts 'num' based on 'size'.
 * @numz: The unsigned number to be converted.
 * @sizez: The size specifier.
 * Return: The converted unsigned number.
 */

unsigned long int convert_size_unsgnd(unsigned long int numz, int sizez)

{
	UNUSED(sizez);
	/*
	 * Implementation for converting 'num' based on 'size' goes here
	 */
	return (numz);
}
